version: '3.8'

services:
  web:
    image: python:3.11-alpine
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "80:80"
    command: python -m http.server 80
    restart: unless-stopped
